Ghost Collision Regression Fix + Grow Light "Space Occupied" (Aug 2026)

Fix 1 — Core: Reorder `_spawn_ghost()` so ghost root enters SceneTree BEFORE
`_rebuild_ghost_mesh()` runs. `strip_collision()` now runs AFTER `add_child(real_inst)` fires `_ready()`, so the strip sticks (scripts' `_ready()` sets collision_layer unconditionally, previously undoing the premature strip).

Fix 2 — Hardening: Added deferred re-strip (`call_deferred`) in `_rebuild_ghost_mesh()` to catch scripts configuring collision via `call_deferred` in `_ready()`. Added `is_instance_valid` guard in `strip_collision()` for safety when ghost freed same frame.

Fix 3 — Grow Light bucket: Preview-only grow lights no longer register into `GrowLight._bucket_registry`. Moved `_is_preview_only` guard before `_register_bucket()` call.

Docs updated:
- `docs/systems/build/README.md`: `strip_collision()` lifecycle rule updated; Call graph shows new `_spawn_ghost()` order (add-to-tree → rebuild)
- `docs/systems/farming/README.md`: Note that preview-only grow lights no longer register into spatial bucket
- `HANDOVER.md`: New session entry with root cause, fixes 1-3, files touched, verification checklist

Root cause: Ghost collision strip ran BEFORE subtree entered SceneTree → `_ready()` re-enabled collision on every spawn. Symptoms: (1) Ghosts push player/NPCs, (2) Grow lights always red/"space occupied" — ghost's own live collider at query position blocked itself.

Verification checklist provided.
Architecture.json regenerated.